import { InterfaceDeclaration } from "../../../src/main";
import { WrapperFactory } from "../WrapperFactory";
export declare class Structure {
    private readonly wrapperFactory;
    private readonly node;
    constructor(wrapperFactory: WrapperFactory, node: InterfaceDeclaration);
    getName(): string;
    getFilePath(): string;
    isOverloadStructure(): boolean;
    getBaseStructures(): Structure[];
    getDescendantBaseStructures(): Structure[];
}
