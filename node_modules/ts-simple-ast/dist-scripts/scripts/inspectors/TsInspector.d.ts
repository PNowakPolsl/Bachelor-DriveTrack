import Project, { SourceFile, SyntaxKind } from "../../src/main";
import { TsNode } from "./ts";
import { WrapperFactory } from "./WrapperFactory";
export declare class TsInspector {
    private readonly wrapperFactory;
    private readonly project;
    constructor(wrapperFactory: WrapperFactory, project: Project);
    getDeclarationFile(): SourceFile;
    getTsNodes(): TsNode[];
    getNamesFromKind(kind: SyntaxKind): string[];
    private getKindToNameMappings();
}
