import { Node, PropertySignature } from "../../../src/main";
import { WrapperFactory } from "../WrapperFactory";
import { TsNode } from "./TsNode";
export declare class TsNodeProperty {
    private readonly wrapperFactory;
    private readonly node;
    constructor(wrapperFactory: WrapperFactory, node: PropertySignature);
    getName(): string;
    getTsNode(): TsNode;
    isReferenced(): boolean;
    findReferencedNodes(): Node[];
}
