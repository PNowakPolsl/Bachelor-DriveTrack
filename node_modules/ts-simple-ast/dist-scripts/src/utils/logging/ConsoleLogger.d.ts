import { EnableableLogger } from "./EnableableLogger";
export declare class ConsoleLogger extends EnableableLogger {
    protected logInternal(text: string): void;
    protected warnInternal(text: string): void;
}
