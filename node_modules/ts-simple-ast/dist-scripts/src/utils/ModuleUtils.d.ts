import { Symbol, SourceFile } from "../compiler";
export declare class ModuleUtils {
    private constructor();
    static isModuleSpecifierRelative(text: string): boolean;
    static getReferencedSourceFileFromSymbol(symbol: Symbol): SourceFile | undefined;
}
