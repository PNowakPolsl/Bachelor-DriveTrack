"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var toAbsoluteGlob = require("@dsherret/to-absolute-glob");
var multimatch = require("multimatch");
function matchGlobs(paths, patterns, cwd) {
    if (typeof patterns === "string")
        patterns = toAbsoluteGlob(patterns, { cwd: cwd });
    else
        patterns = patterns.map(function (p) { return toAbsoluteGlob(p, { cwd: cwd }); });
    return multimatch(paths, patterns);
}
exports.matchGlobs = matchGlobs;
