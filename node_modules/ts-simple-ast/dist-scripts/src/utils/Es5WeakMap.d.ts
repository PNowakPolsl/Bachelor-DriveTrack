export interface WeakDictionary<K extends object, V> {
    get(key: K): V | undefined;
    set(key: K, value: V): void;
    has(key: K): boolean;
    delete(key: K): void;
}
export declare class Es5WeakMap<K extends object, V> implements WeakDictionary<K, V> {
    private propSaver;
    get(key: K): V | undefined;
    set(key: K, value: V): void;
    has(key: K): boolean;
    delete(key: K): void;
}
