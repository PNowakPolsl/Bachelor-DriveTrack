import { CompilerFactory } from "../../factories";
import { SourceFile } from "../../compiler";
/**
 * Updates all the source file's reference containers.
 */
export declare class LazyReferenceCoordinator {
    private dirtySourceFiles;
    constructor(factory: CompilerFactory);
    refreshDirtySourceFiles(): void;
    refreshSourceFileIfDirty(sourceFile: SourceFile): void;
    clearDirtySourceFiles(): void;
    clearDityForSourceFile(sourceFile: SourceFile): void;
}
