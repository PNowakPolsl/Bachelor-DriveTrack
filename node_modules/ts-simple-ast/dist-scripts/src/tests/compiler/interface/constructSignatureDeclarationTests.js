"use strict";
var __assign = (this && this.__assign) || Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
    }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var testHelpers_1 = require("../testHelpers");
describe("ConstructSignatureDeclaration", function () {
    function getFirstConstructSignatureWithInfo(code) {
        var opts = testHelpers_1.getInfoFromText(code);
        return __assign({}, opts, { firstConstructSignature: opts.firstChild.getConstructSignatures()[0] });
    }
    describe("fill", function () {
        function doTest(code, structure, expectedCode) {
            var _a = getFirstConstructSignatureWithInfo(code), firstConstructSignature = _a.firstConstructSignature, sourceFile = _a.sourceFile;
            firstConstructSignature.fill(structure);
            chai_1.expect(sourceFile.getFullText()).to.equal(expectedCode);
        }
        it("should not change when nothing is set", function () {
            doTest("interface Identifier { new(): any; }", {}, "interface Identifier { new(): any; }");
        });
        it("should change when setting", function () {
            doTest("interface Identifier { new(): any; }", { returnType: "string" }, "interface Identifier { new(): string; }");
        });
    });
    describe("remove", function () {
        function doTest(code, indexToRemove, expectedCode) {
            var _a = testHelpers_1.getInfoFromText(code), firstChild = _a.firstChild, sourceFile = _a.sourceFile;
            firstChild.getConstructSignatures()[indexToRemove].remove();
            chai_1.expect(sourceFile.getFullText()).to.equal(expectedCode);
        }
        it("should remove when it's the only member", function () {
            doTest("interface Identifier {\n    new(): string;\n}", 0, "interface Identifier {\n}");
        });
        it("should remove when it's the first member", function () {
            doTest("interface Identifier {\n    new(): string;\n    prop: string;\n    new(): string;\n}", 0, "interface Identifier {\n    prop: string;\n    new(): string;\n}");
        });
        it("should remove when it's the middle member", function () {
            doTest("interface Identifier {\n    new(): string;\n    new(): number;\n    new(): Date;\n}", 1, "interface Identifier {\n    new(): string;\n    new(): Date;\n}");
        });
        it("should remove when it's the last member", function () {
            doTest("interface Identifier {\n    new(): string;\n    new(): number;\n}", 1, "interface Identifier {\n    new(): string;\n}");
        });
        it("should only remove the new signature specified", function () {
            doTest("interface Identifier {\n    new(): string;\n    new(param: number): string;\n    new(t: string): string;\n}", 1, "interface Identifier {\n    new(): string;\n    new(t: string): string;\n}");
        });
    });
});
