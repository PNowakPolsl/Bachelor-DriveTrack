"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var typescript_1 = require("../../../typescript");
var testHelpers_1 = require("../testHelpers");
function getStatement(text) {
    return testHelpers_1.getInfoFromTextWithDescendant(text, typescript_1.SyntaxKind.ForInStatement).descendant;
}
describe("ForInStatement", function () {
    var expression = "{}";
    var initializer = "x";
    var statement = "for (" + initializer + " in " + expression + ") {}";
    describe("getExpression", function () {
        function doTest(text, expectedText) {
            var forInStatement = getStatement(text);
            chai_1.expect(forInStatement.getExpression().getText()).to.equal(expectedText);
        }
        it("should get the correct expression", function () {
            doTest(statement, expression);
        });
    });
    describe("getInitializer", function () {
        function doTest(text, expectedText) {
            var forInStatement = getStatement(text);
            chai_1.expect(forInStatement.getInitializer().getText()).to.equal(expectedText);
        }
        it("should get the correct initializer", function () {
            doTest(statement, initializer);
        });
    });
});
