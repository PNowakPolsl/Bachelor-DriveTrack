"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var typescript_1 = require("../../../typescript");
var testHelpers_1 = require("../testHelpers");
function getStatement(text) {
    return testHelpers_1.getInfoFromTextWithDescendant(text, typescript_1.SyntaxKind.WithStatement).descendant;
}
describe("WithStatement", function () {
    describe("getExpression", function () {
        function doTest(text, expectedText) {
            var withStatement = getStatement(text);
            chai_1.expect(withStatement.getExpression().getText()).to.equal(expectedText);
        }
        it("should get the correct statement", function () {
            doTest("with (Math) { PI; }", "Math");
        });
    });
    describe("getStatement", function () {
        function doTest(text, expectedText) {
            var withStatement = getStatement(text);
            chai_1.expect(withStatement.getStatement().getText()).to.equal(expectedText);
        }
        it("should get the correct statement", function () {
            doTest("with (Math) { PI; }", "{ PI; }");
        });
    });
});
