"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var typescript_1 = require("../../../typescript");
var testHelpers_1 = require("../testHelpers");
function getCaseClause(text) {
    return testHelpers_1.getInfoFromTextWithDescendant(text, typescript_1.SyntaxKind.CaseClause).descendant;
}
describe("CaseClause", function () {
    var expression = "1";
    var statement = "let x = 1 + 2;";
    var clause = "switch (y) { case " + expression + ": " + statement + " }";
    describe("getStatements", function () {
        function doTest(text, expectedText) {
            var caseClause = getCaseClause(text);
            chai_1.expect(caseClause.getStatements()[0].getText()).to.equal(expectedText);
        }
        it("should get the correct statements", function () {
            doTest(clause, statement);
        });
    });
    describe("getExpression", function () {
        function doTest(text, expectedText) {
            var caseClause = getCaseClause(text);
            chai_1.expect(caseClause.getExpression().getText()).to.equal(expectedText);
        }
        it("should get the correct expression", function () {
            doTest(clause, expression);
        });
    });
});
