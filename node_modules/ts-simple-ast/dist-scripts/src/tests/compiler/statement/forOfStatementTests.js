"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var typescript_1 = require("../../../typescript");
var testHelpers_1 = require("../testHelpers");
function getStatement(text) {
    return testHelpers_1.getInfoFromTextWithDescendant(text, typescript_1.SyntaxKind.ForOfStatement).descendant;
}
describe("ForOfStatement", function () {
    var expression = "{}";
    var initializer = "x";
    var statement = "for (" + initializer + " of " + expression + ") {}";
    describe("getExpression", function () {
        function doTest(text, expectedText) {
            var forOfStatement = getStatement(text);
            chai_1.expect(forOfStatement.getExpression().getText()).to.equal(expectedText);
        }
        it("should get the correct expression", function () {
            doTest(statement, expression);
        });
    });
    describe("getInitializer", function () {
        function doTest(text, expectedText) {
            var forOfStatement = getStatement(text);
            chai_1.expect(forOfStatement.getInitializer().getText()).to.equal(expectedText);
        }
        it("should get the correct initializer", function () {
            doTest(statement, initializer);
        });
    });
});
