"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var typescript_1 = require("../../../typescript");
var testHelpers_1 = require("../testHelpers");
function getStatement(text) {
    return testHelpers_1.getInfoFromTextWithDescendant(text, typescript_1.SyntaxKind.DoStatement).descendant;
}
describe("DoStatement", function () {
    var statement = "do {}";
    var expression = "x > 0";
    var expressionStatement = "do {} while (" + expression + ");";
    describe("getExpression", function () {
        function doTest(text, expectedText) {
            var doStatement = getStatement(text);
            chai_1.expect(doStatement.getExpression().getText()).to.equal(expectedText);
        }
        it("should have an empty expression without while", function () {
            doTest(statement, "");
        });
        it("should return the correct expression", function () {
            doTest(expressionStatement, expression);
        });
    });
});
