"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var typescript_1 = require("../../../typescript");
var testHelpers_1 = require("../testHelpers");
function getStatement(text) {
    return testHelpers_1.getInfoFromTextWithDescendant(text, typescript_1.SyntaxKind.LabeledStatement).descendant;
}
describe("LabeledStatement", function () {
    var statement = "let x = 1 + 2";
    var label = "foo";
    var labeled = label + ": " + statement;
    describe("getLabel", function () {
        function doTest(text, expectedText) {
            var labeledStatement = getStatement(text);
            chai_1.expect(labeledStatement.getLabel().getText()).to.equal(expectedText);
        }
        it("should get the correct label", function () {
            doTest(labeled, label);
        });
    });
    describe("getStatement", function () {
        function doTest(text, expectedText) {
            var labeledStatement = getStatement(text);
            chai_1.expect(labeledStatement.getStatement().getText()).to.equal(expectedText);
        }
        it("should get the correct statement", function () {
            doTest(labeled, statement);
        });
    });
});
