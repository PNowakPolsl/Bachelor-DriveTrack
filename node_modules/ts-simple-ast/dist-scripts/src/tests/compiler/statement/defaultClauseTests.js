"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var typescript_1 = require("../../../typescript");
var testHelpers_1 = require("../testHelpers");
function getDefaultClause(text) {
    return testHelpers_1.getInfoFromTextWithDescendant(text, typescript_1.SyntaxKind.DefaultClause).descendant;
}
describe("DefaultClause", function () {
    var statement = "let x = 1 + 2;";
    var clause = "switch (x) { default: " + statement + " }";
    describe("getStatements", function () {
        function doTest(text, expectedText) {
            var defaultClause = getDefaultClause(text);
            chai_1.expect(defaultClause.getStatements()[0].getText()).to.equal(expectedText);
        }
        it("should get the correct statements", function () {
            doTest(clause, statement);
        });
    });
});
