"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var testHelpers_1 = require("../testHelpers");
describe("ExportAssignment", function () {
    describe("isExportEquals", function () {
        function doTest(text, expected) {
            var firstChild = testHelpers_1.getInfoFromText(text).firstChild;
            chai_1.expect(firstChild.isExportEquals()).to.equal(expected);
        }
        it("should be an export equals when is one", function () {
            doTest("export = 5;", true);
        });
        it("should not be an export equals when not one", function () {
            doTest("export default 5;", false);
        });
    });
    describe("getExpression", function () {
        function doTest(text, expected) {
            var firstChild = testHelpers_1.getInfoFromText(text).firstChild;
            chai_1.expect(firstChild.getExpression().getText()).to.equal(expected);
        }
        it("should get the expression for an export equals", function () {
            doTest("export = 5;", "5");
        });
        it("should get the expression for an export default", function () {
            doTest("export default 5;", "5");
        });
    });
});
