"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var typescript_1 = require("../../../typescript");
var testHelpers_1 = require("../testHelpers");
function getInfo(text) {
    return testHelpers_1.getInfoFromTextWithDescendant(text, typescript_1.SyntaxKind.JsxText, { isJsx: true });
}
describe("JsxText", function () {
    describe("containsOnlyWhiteSpaces", function () {
        function doTest(text, value) {
            var descendant = getInfo(text).descendant;
            chai_1.expect(descendant.containsOnlyWhiteSpaces()).to.equal(value);
        }
        it("should get if it contains only whitespace when it doesn't", function () {
            doTest("var t = (<jsx>Test</jsx>);", false);
        });
        it("should get if it contains only whitespace when it does", function () {
            doTest("var t = (<jsx>\n    <Test /></jsx>);", true);
        });
    });
});
