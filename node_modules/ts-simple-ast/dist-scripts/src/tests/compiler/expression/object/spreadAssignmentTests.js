"use strict";
var __assign = (this && this.__assign) || Object.assign || function(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
            t[p] = s[p];
    }
    return t;
};
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var typescript_1 = require("../../../../typescript");
var testHelpers_1 = require("../../testHelpers");
describe("SpreadAssignment", function () {
    function getSpreadAssignment(text) {
        var opts = testHelpers_1.getInfoFromText(text);
        var spreadAssignment = opts.sourceFile.getFirstDescendantByKindOrThrow(typescript_1.SyntaxKind.SpreadAssignment);
        return __assign({ spreadAssignment: spreadAssignment }, opts);
    }
    describe("getExpression", function () {
        it("should get the spread assignment expression", function () {
            var spreadAssignment = getSpreadAssignment("const t = { ...obj }").spreadAssignment;
            chai_1.expect(spreadAssignment.getExpression().getText()).to.equal("obj");
        });
    });
});
