"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var utils_1 = require("../../utils");
describe("Es5HashSet", function () {
    it("should add values to the hash set and say they exist when they do", function () {
        var hashSet = new utils_1.Es5HashSet();
        chai_1.expect(utils_1.ArrayUtils.from(hashSet.values())).to.deep.equal([]);
        chai_1.expect(hashSet.has("")).to.be.false;
        hashSet.add("");
        chai_1.expect(utils_1.ArrayUtils.from(hashSet.values())).to.deep.equal([""]);
        chai_1.expect(hashSet.has("")).to.be.true;
        chai_1.expect(hashSet.delete("")).to.be.true;
        chai_1.expect(hashSet.delete("")).to.be.false;
        chai_1.expect(utils_1.ArrayUtils.from(hashSet.values())).to.deep.equal([]);
        chai_1.expect(hashSet.has("")).to.be.false;
    });
    describe("clear", function () {
        it("should clear the added values", function () {
            var hashSet = new utils_1.Es5HashSet();
            hashSet.add(1);
            hashSet.add(2);
            hashSet.clear();
            chai_1.expect(utils_1.ArrayUtils.from(hashSet.values())).to.deep.equal([]);
        });
    });
});
