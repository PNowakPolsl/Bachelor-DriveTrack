"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var utils_1 = require("../../utils");
describe("Es5Map", function () {
    // todo: organize these tests...
    function doTest(map, key) {
        chai_1.expect(map.get(key)).to.be.undefined;
        chai_1.expect(map.has(key)).to.be.false;
        map.set(key, "value");
        chai_1.expect(map.get(key)).to.equal("value");
        chai_1.expect(map.has(key)).to.be.true;
        chai_1.expect(utils_1.ArrayUtils.from(map.entries())).to.deep.equal([[key, "value"]]);
        map.delete(key);
        chai_1.expect(utils_1.ArrayUtils.from(map.entries())).to.deep.equal([]);
        chai_1.expect(map.get(key)).to.be.undefined;
        chai_1.expect(map.has(key)).to.be.false;
    }
    it("should work with a string", function () {
        doTest(new utils_1.Es5Map(), "key");
    });
    it("should work", function () {
        doTest(new utils_1.Es5Map(), {});
    });
});
