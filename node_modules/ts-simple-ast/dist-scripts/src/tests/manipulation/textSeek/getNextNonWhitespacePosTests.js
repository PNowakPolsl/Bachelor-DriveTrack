"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var textSeek_1 = require("../../../manipulation/textSeek");
describe("getNextNonWhitespacePos", function () {
    function doTest(fileText, pos, expectedPos) {
        chai_1.expect(textSeek_1.getNextNonWhitespacePos(fileText, pos)).to.equal(expectedPos);
    }
    it("should the next non whitespace char", function () {
        var code = " \r\n\t   \t  ";
        var expectedPos = code.length;
        code += "a      \t  aaa";
        doTest(code, 5, expectedPos);
    });
    it("should return the end of the string if only whitespace are found", function () {
        var code = "\r\n   \r   \t \t   \n    ";
        var expectedPos = code.length;
        doTest(code, 0, expectedPos);
    });
});
