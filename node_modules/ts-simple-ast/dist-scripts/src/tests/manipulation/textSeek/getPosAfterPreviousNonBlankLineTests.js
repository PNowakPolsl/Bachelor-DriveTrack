"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var textSeek_1 = require("../../../manipulation/textSeek");
describe("getPosAfterPreviousNonBlankLine", function () {
    function doTest(fileText, pos, expectedPos) {
        chai_1.expect(textSeek_1.getPosAfterPreviousNonBlankLine(fileText, pos)).to.equal(expectedPos);
    }
    it("should get the start line pos after previous non blank line", function () {
        var code = "text \r\n";
        var expectedPos = code.length;
        code += " \r\n    \r\n  \n\t \r\n";
        var pos = code.length;
        code += "more";
        doTest(code, pos, expectedPos);
    });
    it("should get the start of the text if it keeps finding blank lines", function () {
        var expectedPos = 0;
        var code = "\r\n";
        code += " \r\n    \r\n  \n\t \r\n";
        var pos = code.length;
        code += "more";
        doTest(code, pos, expectedPos);
    });
});
