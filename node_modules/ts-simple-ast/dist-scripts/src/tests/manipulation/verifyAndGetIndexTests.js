"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var manipulation_1 = require("../../manipulation");
describe("verifyAndGetIndex", function () {
    it("should get the index when zero and length is zero", function () {
        chai_1.expect(manipulation_1.verifyAndGetIndex(0, 0)).to.equal(0);
    });
    it("should get the index when equal to length", function () {
        chai_1.expect(manipulation_1.verifyAndGetIndex(1, 1)).to.equal(1);
    });
    it("should get the index when equal to length * 1", function () {
        chai_1.expect(manipulation_1.verifyAndGetIndex(-1, 1)).to.equal(0);
    });
    it("should throw the index greater than length", function () {
        chai_1.expect(function () { return manipulation_1.verifyAndGetIndex(2, 1); }).to.throw();
    });
    it("should throw the index greater than length * -1", function () {
        chai_1.expect(function () { return manipulation_1.verifyAndGetIndex(-2, 1); }).to.throw();
    });
});
