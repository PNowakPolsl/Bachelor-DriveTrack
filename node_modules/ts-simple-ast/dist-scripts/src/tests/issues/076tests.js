"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var chai_1 = require("chai");
var testHelpers_1 = require("../compiler/testHelpers");
describe("tests for issue #76", function () {
    it("should rename the class when there's a comment block above", function () {
        var text = "/**\n * Test\n */\nclass Identifier {\n    /** Other text */\n    prop: string;\n}";
        var firstChild = testHelpers_1.getInfoFromText(text).firstChild;
        firstChild.rename("NewName");
        firstChild.getInstanceProperties()[0].rename("newPropName");
        chai_1.expect(firstChild.getFullText()).to.equal(text.replace("Identifier", "NewName").replace("prop", "newPropName"));
    });
});
