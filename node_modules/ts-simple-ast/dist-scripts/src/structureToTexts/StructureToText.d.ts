import CodeBlockWriter from "code-block-writer";
export declare abstract class StructureToText<TStructure> {
    protected readonly writer: CodeBlockWriter;
    constructor(writer: CodeBlockWriter);
    abstract writeText(structure: TStructure): void;
}
