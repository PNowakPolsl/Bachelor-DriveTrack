import CodeBlockWriter from "code-block-writer";
import { MethodDeclarationStructure } from "../../structures";
import { StructureToText } from "../StructureToText";
export declare class MethodDeclarationStructureToText extends StructureToText<MethodDeclarationStructure> {
    private readonly opts;
    private readonly jsDocWriter;
    private readonly modifierWriter;
    private readonly parameterWriter;
    constructor(writer: CodeBlockWriter, opts: {
        isAmbient: boolean;
    });
    writeText(structure: MethodDeclarationStructure): void;
}
