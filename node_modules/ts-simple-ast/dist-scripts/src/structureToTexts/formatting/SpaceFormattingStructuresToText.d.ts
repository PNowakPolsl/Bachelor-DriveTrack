import CodeBlockWriter from "code-block-writer";
import { StructureToText } from "../StructureToText";
export declare class SpaceFormattingStructuresToText<T> extends StructureToText<T[]> {
    private readonly structureToText;
    constructor(writer: CodeBlockWriter, structureToText: StructureToText<T>);
    writeText(structures: T[]): void;
}
